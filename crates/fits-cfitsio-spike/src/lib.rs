//! Throwaway spike for issue #107 — validate stock `fitsio` (CFITSIO
//! bindings) builds and runs on Windows MSVC when CFITSIO is supplied
//! by vcpkg via pkg-config. The crate is empty by design; all spike
//! work lives in `tests/`. See `docs/plans/fits-cfitsio-vcpkg-spike.md`.
