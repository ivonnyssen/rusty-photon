//! FITS reader. Filled in by Phase 3 (delegates to `fitsrs`).

use crate::error::FitsError;

/// Stub. The Phase 3 implementation will land the public surface
/// (`read_primary`, `read_primary_as_i32`, `read_primary_keyword`).
#[doc(hidden)]
pub fn _placeholder() -> Result<(), FitsError> {
    Ok(())
}
