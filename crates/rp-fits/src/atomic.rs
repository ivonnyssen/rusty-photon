//! Atomic write helper. Filled in by Phase 4 (lifted from
//! `services/rp/src/persistence/fits.rs::write_fits_sync`).

use crate::error::FitsError;

/// Stub. The Phase 4 implementation will land the public surface
/// (`write_atomic`, `write_atomic_with`).
#[doc(hidden)]
pub fn _placeholder() -> Result<(), FitsError> {
    Ok(())
}
