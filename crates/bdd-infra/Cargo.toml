[package]
name = "bdd-infra"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared BDD test infrastructure for spawning, managing, and stopping service binaries"

# Used by integration tests to exercise ServiceHandle against test_service binary
[package.metadata.bdd]
env_var = "TEST_SERVICE_BINARY"

[lints]
workspace = true

[dependencies]
libc = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
toml = "1.1.0"
tokio = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
