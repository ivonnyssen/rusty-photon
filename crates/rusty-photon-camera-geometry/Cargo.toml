[package]
name = "rusty-photon-camera-geometry"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Sensor-geometry arithmetic shared by the ASCOM camera drivers: ROI validation, bin rescaling, binned-full-frame alignment"
rust-version.workspace = true

[lints]
workspace = true

# No dependencies, deliberately. Everything here is arithmetic over `u32` and
# `Duration`; a vendor type or an `ascom-alpaca` type appearing in a signature
# would be the signal that the item belongs in a driver instead.
[dependencies]
