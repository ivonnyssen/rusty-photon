//! Doctor-subcommand smoke steps — all shared, generated against the
//! `DoctorSmokeWorld` impl in `world.rs`.

use crate::world::SkySurveyCameraWorld;

bdd_infra::doctor_smoke_steps!(SkySurveyCameraWorld);
