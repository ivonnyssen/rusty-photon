//! Human-readable report rendering — re-exported from
//! `rusty_photon_doctor_checks::render`, which knows every report mode
//! (central packaged/config-only and the per-service `mode: service`).

pub use rusty_photon_doctor_checks::render::*;
